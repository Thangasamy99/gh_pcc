import React from 'react';
import { Box, Typography } from '@mui/material';

const SystemHealth: React.FC = () => {
    return (
        <Box sx={{ height: 300, display: 'flex', alignItems: 'center', justifyContent: 'center', bgcolor: 'background.default', borderRadius: 2 }}>
            <Typography color="text.secondary">System Health component coming soon...</Typography>
        </Box>
    );
};

export default SystemHealth;
